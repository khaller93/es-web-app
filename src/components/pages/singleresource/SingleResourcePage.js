import React from "react";
import Page from "../Page";

/**
 * This React page displays an instance according to a given reactor
 * configuration.
 */
const SingleResourcePage = ({app, intl}) => {

    return (
      <Page>
          Instance
      </Page>
    );
};


export default SingleResourcePage;